apiVersion: v1
kind: Service
metadata:
  name: {{ .app_name }}
  namespace: {{ .app_name }}-ns
spec:
  selector:
    app: {{ .app_name }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: {{ .app_port }}
